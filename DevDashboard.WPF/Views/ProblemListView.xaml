<UserControl x:Class="DevDashboard.WPF.Views.ProblemListView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- Toolbar -->
        <StackPanel Grid.Row="0" Orientation="Horizontal" Margin="0,0,0,16">
            <Button Content="새로고침" 
                    Command="{Binding LoadCommand}"
                    Padding="16,8"/>

            <Button Content="삭제" 
                    Command="{Binding DeleteCommand}"
                    Margin="8,0,0,0"
                    Padding="16,8"/>
        </StackPanel>

        <!-- Loading -->
        <TextBlock Grid.Row="1" 
                   Text="로딩 중..." 
                   Visibility="{Binding IsLoading, Converter={StaticResource BoolToVisibility}}"
                   Foreground="White"
                   FontSize="16"/>

        <!-- Problem List -->
        <DataGrid Grid.Row="1" 
                  ItemsSource="{Binding Problems}"
                  SelectedItem="{Binding SelectedProblem}"
                  AutoGenerateColumns="False"
                  IsReadOnly="True"
                  SelectionMode="Single"
                  Background="#2D2D30"
                  Foreground="White"
                  BorderThickness="0">

            <DataGrid.Columns>
                <DataGridTextColumn Header="출처" Binding="{Binding Source}" Width="100"/>
                <DataGridTextColumn Header="번호" Binding="{Binding ProblemNo}" Width="80"/>
                <DataGridTextColumn Header="제목" Binding="{Binding Title}" Width="*"/>
                <DataGridTextColumn Header="난이도" Binding="{Binding Difficulty}" Width="100"/>
                <DataGridTextColumn Header="풀이 수" Binding="{Binding SolutionCount}" Width="80"/>
            </DataGrid.Columns>
        </DataGrid>
    </Grid>
</UserControl>